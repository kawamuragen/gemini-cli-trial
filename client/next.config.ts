import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  output: 'export',
  basePath: '/gemini-cli-trial',
  assetPrefix: '/gemini-cli-trial/',
  /* config options here */
};

export default nextConfig;
